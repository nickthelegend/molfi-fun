{
  "name": "molfi",
  "private": true,
  "version": "2.0.0",
  "description": "Prediction markets where your position stays sealed until settlement, on Starknet through the STRK20 privacy pool",
  "license": "MIT",
  "packageManager": "pnpm@10.29.2",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev": "pnpm --filter @molfi/hub dev"
  },
  "pnpm": {
    "overrides": {
      "@swc/core": "1.13.5"
    }
  }
}
